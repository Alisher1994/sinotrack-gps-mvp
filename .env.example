PORT=3000
TRACKER_PORT=5001
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/gps_platform
DATABASE_SSL=false

# Optional raw response sent to tracker after each TCP packet.
# Example hex values:
# TRACKER_ACK_HEX=0d0a
# TRACKER_ACK_ASCII=OK
TRACKER_ACK_HEX=
TRACKER_ACK_ASCII=
